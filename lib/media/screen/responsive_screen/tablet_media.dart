import 'package:flutter/material.dart';

class TabletMedia extends StatelessWidget {
  const TabletMedia({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Tablet Media');
  }
}
