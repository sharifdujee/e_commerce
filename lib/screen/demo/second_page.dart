import 'package:flutter/material.dart';
class SecondPage extends StatelessWidget {
  const SecondPage({super.key});
  static const secondPage = '/second-screen';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
